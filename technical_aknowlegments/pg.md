# PostgreSQL — ключевые функции с краткими определениями

## 📌 Базовые функции

### **COALESCE(x, y, …)**
Возвращает первое ненулевое значение.

### **NULLIF(a, b)**
Возвращает NULL, если `a = b`.

### **GREATEST(a, b, …)** / **LEAST(a, b, …)**
Возвращает максимум / минимум из списка аргументов.

---

## 📌 Строковые функции

### **CONCAT(a, b, …)**
Объединяет строки, автоматически приводит типы к тексту.

### **LOWER() / UPPER()**
Приведение регистра.

### **TRIM(), LTRIM(), RTRIM()**
Удаление пробелов или символов по краям строки.

### **SUBSTRING(str FROM pattern)**
Извлечение подстроки.

### **POSITION(substr IN str)**
Поиск позиции подстроки.

### **REGEXP_REPLACE()**
Замена по регулярному выражению.

### **REGEXP_MATCHES()**
Поиск соответствий по регулярному выражению.

---

## 📌 Числовые функции

### **ROUND(), CEIL(), FLOOR()**
Округление числа различными способами.

### **ABS(x)**
Модуль числа.

### **RANDOM()**
Генерирует псевдослучайное число от 0 до 1.

---

## 📌 Дата и время

### **NOW(), CURRENT_TIMESTAMP**
Текущее время.

### **AGE(a, b)**
Разница между датами (возвращает интервал).

### **DATE_TRUNC('unit', ts)**
Обрезает timestamp до нужной точности.

### **EXTRACT(field FROM ts)**
Извлечение части даты/времени (год, месяц, день и т. д.).

---

## 📌 Агрегатные функции

### **COUNT()**
Количество строк.

### **SUM(), AVG(), MAX(), MIN()**
Базовые агрегаты.

### **STRING_AGG(str, sep)**
Объединяет несколько строк в одну.

### **ARRAY_AGG()**
Собирает значения в массив.

### **JSON_AGG(), JSONB_AGG()**
Агрегирует в JSON-массив.

---

## 📌 Функции работы с массивами

### **unnest(array)**
Разворачивает массив в набор строк.

### **array_length(a, dim)**
Возвращает длину массива.

### **array_append(a, elem)**  
Добавляет элемент в конец массива.

### **array_prepend(a, elem)**  
Добавляет элемент в начало массива.

### **array_remove(a, elem)**
Удаляет элементы, равные `elem`.

### **array_position(a, elem)**
Находит индекс элемента.

### **array_to_string(a, sep)**
Массив → строка.

### **string_to_array(str, sep)**
Строка → массив.

---

## 📌 JSON / JSONB функции

### **->**  
Извлекает JSON-объект или массив.

### **->>**  
Извлекает текстовое значение.

### **#>**  
Извлекает по JSON-пути.

### **#>>**  
Извлекает по пути как текст.

### **jsonb_build_object()**
Конструирует JSON-объект из пар ключ-значение.

### **jsonb_build_array()**
Создаёт JSON-массив.

### **jsonb_set(json, path, value)**
Изменяет JSON по указанному пути.

### **jsonb_extract_path()**
Извлекает значение по пути.

### **jsonb_array_elements()**
Разворачивает JSON-массив в строки.

---

## 📌 Поиск и индексация

### **to_tsvector(text)**  
Преобразует текст для полнотекстового поиска.

### **to_tsquery(query)**  
Формирует поисковый запрос.

### **ts_rank()**
Оценивает релевантность найденного текста.

---

## 📌 Оконные функции

### **ROW_NUMBER(), RANK(), DENSE_RANK()**
Нумерация строк разными способами.

### **LAG(col), LEAD(col)**
Доступ к предыдущей или следующей строке.

### **SUM() OVER (), AVG() OVER ()**
Агрегаты поверх окна (без группировки).

### **FIRST_VALUE(), LAST_VALUE()**
Первое/последнее значение в окне.

---

## 📌 Системные и утилитарные функции

### **pg_sleep(n)**
Пауза выполнения — полезно для тестов.

### **current_database(), current_user**
Информация о текущей сессии.

### **version()**
Версия PostgreSQL.

---

## 📌 Геометрия и расширения

### **cube, earthdistance**
Работа с координатами и расстояниями по сфере.

### **PostGIS (если установлен)**
Функции для геоданных: `ST_Distance`, `ST_Intersects` и т.д.
